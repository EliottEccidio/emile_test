Implement "$ARGUMENTS" test-first:
1. Write a failing test that captures the desired behavior (happy path + 1 edge). Run it, show it RED.
2. Write the minimum code to make it pass. Run it, show it GREEN.
3. Refactor only if needed; tests stay green.
Keep it surgical. Don't touch unrelated code.
