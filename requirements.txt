# Fine-tuning LoRA de Qwen2.5-1.5B-Instruct sur GSM8K + observabilite MLflow
# Pas de bitsandbytes : incompatible avec Apple Silicon (MPS). Quantization desactivee.

torch>=2.2
transformers>=4.46,<5
datasets>=3.0
peft>=0.13
trl>=0.12
accelerate>=1.0
mlflow>=2.16
pyyaml>=6.0
tqdm>=4.66
pytest>=8.0
